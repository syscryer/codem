# 临时记录：Agent 安装/更新状态

> 临时文档。用户确认相关问题不再需要追踪后，可直接删除本文件。

## 记录时间

2026-08-14

## 已处理内容

Agent 设置页的安装和更新流程已增加统一状态展示，适用于走统一生命周期接口的 Agent Provider。

- 安装中显示“正在安装”和旋转图标。
- 更新中显示“正在更新”和旋转图标。
- 安装或更新期间按钮自动禁用，不能重复点击。
- 切换 Provider 或刷新页面后，从后端恢复真实操作状态。
- 操作结束后立即恢复按钮，后续版本检测和 ACP 检测在后台继续执行。
- 每个 Provider 独立维护安装状态，互不阻塞。
- 窗口关闭或请求中断时，后端自动释放安装锁，避免永久卡在“安装中”。

## 修复的问题

1. 快速双击安装按钮可能发出两个请求，第二个请求返回“该 Agent 正在执行安装或更新”，并提前清掉第一个请求的界面状态。
2. 页面刷新或切换 Provider 后，本地 React 状态丢失，无法知道后端是否仍在安装。
3. 安装命令已经完成，但后续 Grok ACP 检测较慢，界面仍长时间显示“更新中”。
4. 生命周期请求被取消时，后端安装锁可能没有执行清理，导致后续操作一直被认为正在运行。

## 验证方式

已通过：

- `npm run typecheck`
- `npm run build`
- `cargo test --lib agent_lifecycle`
- `cargo fmt --all -- --check`
- `git diff --check`

桌面开发模式运行目录：`D:\ai_proj\codem`

## 手工验证建议

1. 在 Agent 设置中选择一个支持安装或更新的 Provider。
2. 点击安装或更新，确认状态变为“正在安装”或“正在更新”，按钮不可点击。
3. 快速连续点击按钮，确认不会出现重复请求或错误提示。
4. 切换到其他 Provider 再切回来，确认状态仍与实际操作一致。
5. 操作完成后确认按钮恢复，版本状态更新为“已安装”或“已是最新”。

